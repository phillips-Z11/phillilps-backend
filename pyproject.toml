[project]
name = "phillips-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.124.4",
    "jinja2>=3.1.6",
    "uvicorn>=0.33.0",
]

[tool.vercel]
entrypoint = "backend.server:app"
